export type RecommendedItemType = {
  itemId: number;
  title: string;
  imagePath: number; // 추후 string으로 변경
  price: number;
};

export type PopularItemType = {
  itemId: number;
  title: string;
  imagePath: number; // 추후 string으로 변경
  price: number;
};
