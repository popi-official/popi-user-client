export type PostPopUpSearch = {
  popupId: number;
  popupName: string;
  imageUrl: string;
  popupOpenDate: string;
  popupCloseDate: string;
  address: string;
};

export type PostItemSearch = {
  itemId: number;
  name: string;
  imageUrl: string;
  price: number;
};
